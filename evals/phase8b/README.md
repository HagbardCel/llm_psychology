# Phase 8B — closed server-scheduling benchmark

Phase 8B measured how OpenAI-compatible local inference servers execute the
concurrent `eval-report` workload on a frozen Qwen3.8-27B fixture. The experiment
is **closed**; operator scripts were removed in Phase 8 lean closure.

Frozen measurements and rationale: [`OUTCOME.md`](OUTCOME.md).

For current backend comparison, see **Evaluating a new local backend** in
[`evals/README.md`](../README.md) and [`docs/development.md`](../../docs/development.md).
