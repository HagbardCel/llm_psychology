# Environment Configuration Example
# Copy this file to .env and fill in the values
#
# ENVIRONMENT FILES:
# - .env          -> Development/Production (copy this file)
# - .env.test     -> Automated testing (pre-configured)
# - .env.usertest -> Manual testing mode (copy your API key there)

# ============================================
# API Keys (REQUIRED)
# ============================================

# Google Gemini API Key (Required)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# LLM Model Configuration (Optional)
# ============================================

# Default model for all agents
# Default: gemini-2.5-flash
# MODEL_NAME=gemini-2.5-flash

# Agent-Specific LLM Models (Optional)
# Configure different models for different agents to optimize cost/performance.
# If not set, all agents use MODEL_NAME as the default.
#
# Recommended configuration for cost optimization:
# - Use gemini-2.5-flash for simple agents (intake, reflection, memory, planning)
# - Use gemini-2.5-pro for complex agents (assessment, psychoanalyst)
#
# INTAKE_MODEL=gemini-2.5-flash
# ASSESSMENT_MODEL=gemini-2.5-pro
# PSYCHOANALYST_MODEL=gemini-2.5-pro
# REFLECTION_MODEL=gemini-2.5-flash
# MEMORY_MODEL=gemini-2.5-flash
# PLANNING_MODEL=gemini-2.5-flash

# ============================================
# Application Environment (Optional)
# ============================================

# Options: production, development, testing
# Default: production
# - production: Normal operation, full session duration (45 min)
# - development: Same as production but with dev tools enabled
# - testing: Used by automated tests, short sessions (1 min)
APP_ENV=production

# ============================================
# Logging Configuration (Optional)
# ============================================

# File logging level (saved to logs/app.log)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL=INFO

# Console logging level (shown in terminal)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: CRITICAL (minimal console output)
CONSOLE_LOG_LEVEL=CRITICAL

# ============================================
# Database Configuration (Optional)
# ============================================

# SQLite database file path
# Default: data/psychoanalyst.db
# DATABASE_PATH=data/psychoanalyst.db

# Vector database path (for RAG)
# Default: data/vector_db
# VECTOR_DB_PATH=data/vector_db

# Connection pool settings
DATABASE_POOL_SIZE=5
DATABASE_POOL_TIMEOUT=30

# ============================================
# Session Configuration (Optional)
# ============================================

# Session duration in minutes
# Default: 45
# SESSION_DURATION_MINUTES=45

# Maximum concurrent sessions
# MAX_CONCURRENT_SESSIONS=10

# Session timeout in minutes
# SESSION_TIMEOUT_MINUTES=60

# ============================================
# Embedding Configuration (Optional)
# ============================================

# Use ONNX backend for faster embeddings
# USE_ONNX_EMBEDDINGS=true

# Embedding model name
# EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2